# Console (Next.js)

边端控制台：配置 / 查单开关 / 白名单。默认 `http://127.0.0.1:18790`。

```bash
# 仓库根
npm install
npm run console          # next dev
npm run console:build
npm run console:start
```

知识库上传仍可用旧版 `npm run admin:legacy`（`scripts/kb-admin-server.js` + `admin/`），后续迁入本应用。
